"""Optional integrations with specific gateway/runtime frameworks.

Each submodule imports its host framework lazily and is only usable
when that framework is installed. Install integrations via extras::

    pip install mcp-schema-normalize[litellm]
"""
