## What does this PR do?
<!-- One sentence. -->

## Type
- [ ] Bug fix
- [ ] Feature
- [ ] Refactor / cleanup
- [ ] Docs
- [ ] Tests only

## Testing
- [ ] Added/updated unit tests (`tests/`)
- [ ] `uv run pytest` passes locally
- [ ] If the change touches the LiteLLM integration, tested against a LiteLLM proxy
- [ ] If the change introduces a new lossy rewrite, added a new telemetry counter + WARN-level log

## Breaking changes?
<!--
Does this change the public API (anything exported from `mcp_schema_normalize` or
`mcp_schema_normalize.integrations.*`), the telemetry counter names, or any module
constant default? List them here.
-->

## Related issues
Closes #
